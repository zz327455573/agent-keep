[build-system]
requires = ["setuptools>=64.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-keep"
version = "1.0.0"
description = "Agent Keep monorepo - persistent CLI agent gateways for Claude Code, Codex, and AGY"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
py-modules = []

